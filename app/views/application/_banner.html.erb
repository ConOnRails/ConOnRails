<div id="banner" class="<%= get_banner_style %>">
  <div id="logo">
    <%= image_tag 'ciab-logo-trans.png', alt: 'Con In a Box' %>
  </div>

  <%= render "banner_text" %>

  <% if is_authenticated? %>
    <a href="/events/new?emergency=1" id="emergency_button" class="<%= get_emerg_button_style %>">Open a New Emergency</a>
      <div>
        Pardon the dust. We're renovating!
      </div>
  <% end %>
</div>
