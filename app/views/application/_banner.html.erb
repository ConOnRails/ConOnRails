<div id="banner" class="<%= get_banner_style %>">
  <div id="logo">
    <%= image_tag 'ciab-logo-trans.png', alt: 'Con In a Box' %>
  </div>

  <%= render "banner_text" %>

  <% if is_authenticated? %>
      <div id="emergency_button" class="button">
        <a href="/events/new?emergency=1" class="button <%= get_emerg_button_style %>">Open a New Emergency</a>
      </div>
      <div>
        Pardon the dust. We're renovating!
      </div>
  <% end %>
</div>
