<%= form_for(@event_status) do |f| %>
  <% if @event_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event_status.errors.count, "error") %> prohibited this event_status from being saved:</h2>

      <ul>
      <% @event_status.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
