version: "3.7"
services:
  db:
    image: postgres:15
    ports:
      - "5432:5432"
    volumes:
      - db:/var/lib/postgresql/data

  app:
    image: con_on_rails:latest
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/app

volumes:
  db:
