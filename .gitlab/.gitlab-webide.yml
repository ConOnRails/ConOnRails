terminal:
  # This can be any image that has the necessary runtime environment for your project.
  image: ruby:2.6
  before_script:
    - apk update
  script: sleep 60
  variables:
    RAILS_ENV: "test"
    NODE_ENV: "test"
